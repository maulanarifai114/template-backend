import { DefaultPaginationPipe } from './default-pagination.pipe';

describe('DefaultPaginationPipe', () => {
  it('should be defined', () => {
    expect(new DefaultPaginationPipe()).toBeDefined();
  });
});
